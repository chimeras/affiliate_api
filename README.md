affiliate_api
=============

This repository contains a starter CodeIgniter 4 LMS implementation with authentication, course/lesson CRUD, quizzes, and notifications. Copy `.env.example` to `.env`, configure your database, run migrations, and serve via `php spark serve`.
